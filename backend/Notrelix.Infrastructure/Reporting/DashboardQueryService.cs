namespace Notrelix.Infrastructure.Reporting;

/// <summary>
/// Skeleton dashboard/reporting query service (v4 §15). Real implementation
/// reads reporting snapshots/projections (not heavy live scans of core work
/// tables) and is permission-aware. Not yet wired.
/// </summary>
public sealed class DashboardQueryService
{
    // TODO(v4 §15): query reporting snapshots, return widget data DTOs.
    // Widget providers handle technical/query only — no Domain invariants.
}
