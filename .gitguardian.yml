version: 2

# Secret-scan policy exceptions.
#
# Every entry here MUST name a justification and stay as narrow as possible.
# Do not add paths to accommodate live/realistic credentials — those must be
# removed or rotated instead.

# MfaTotpServiceTests uses the RFC 6238 reference secret (base32 of ASCII
# "12345678901234567890") — a purely synthetic, published test vector.
# The literal has been removed from the working tree (derived at runtime);
# this ignore only covers historical commits in PR scans for this one file.
ignore_paths:
  - backend/tests/Notrelix.Infrastructure.Tests/Identity/Mfa/MfaTotpServiceTests.cs
