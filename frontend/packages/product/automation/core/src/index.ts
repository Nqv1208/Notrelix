/**
 * @notrelix/automation-core — Automation product core types.
 *
 * Framework-neutral: no React, no DOM.
 */

export type {
  AutomationTriggerType,
  AutomationActionType,
  AutomationRule,
  AutomationRun,
} from './types';
